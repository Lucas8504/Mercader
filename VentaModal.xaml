<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mercader.VentaModal"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Agregar Venta"
    BackgroundColor="#1e1e1e">
    <!--  Fondo oscuro para combinar con MainPage  -->

    <ScrollView>
        <VerticalStackLayout
            Margin="20"
            Padding="20"
            Spacing="20">

            <!--  Título de la página  -->
            <Label
                FontAttributes="Bold"
                FontSize="20"
                HorizontalOptions="Center"
                Text="Agregar Venta"
                TextColor="#cccccc" />

            <!--  Campo de Descripción con autocompletado  -->
            <Frame
                Padding="10"
                BackgroundColor="#2a2a2a"
                CornerRadius="8"
                HasShadow="False">
                <Entry
                    x:Name="DescripcionV_Entry"
                    BackgroundColor="Transparent"
                    Keyboard="Default"
                    Placeholder="Descripción"
                    PlaceholderColor="#7d7c7c"
                    TextColor="#cccccc"
                    TextChanged="OnDescripcionTextChanged" />
            </Frame>

            <!--  Lista de sugerencias para autocompletado  -->
            <Frame
                x:Name="SuggestionsFrame"
                Padding="0"
                BackgroundColor="#2a2a2a"
                CornerRadius="8"
                HasShadow="False"
                IsVisible="False"
                MaximumHeightRequest="200">
                <CollectionView
                    x:Name="SuggestionsView"
                    SelectionMode="Single"
                    SelectionChanged="OnSuggestionSelected">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Grid
                                Padding="12,10"
                                ColumnSpacing="0"
                                BackgroundColor="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnSuggestionTapped" />
                                </Grid.GestureRecognizers>

                                <!-- Texto de la sugerencia -->
                                <Label
                                    Grid.Column="0"
                                    Text="{Binding .}"
                                    TextColor="#cccccc"
                                    FontSize="15"
                                    VerticalOptions="Center" />

                                <!-- Botón para eliminar la sugerencia -->
                                <Label
                                    Grid.Column="1"
                                    Text="✕"
                                    TextColor="#FF5252"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    VerticalOptions="Center"
                                    HorizontalOptions="End"
                                    Padding="8,4">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnDismissSuggestionTapped" />
                                    </Label.GestureRecognizers>
                                </Label>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>

            <!--  Campo de Precio  -->
            <Frame
                Padding="10"
                BackgroundColor="#2a2a2a"
                CornerRadius="8"
                HasShadow="False">
                <Entry
                    x:Name="PrecioEntry"
                    BackgroundColor="Transparent"
                    Keyboard="Numeric"
                    Placeholder="Precio"
                    PlaceholderColor="#7d7c7c"
                    TextColor="#cccccc" />
            </Frame>

            <!--  Campo de Cantidad  -->
            <Frame
                Padding="10"
                BackgroundColor="#2a2a2a"
                CornerRadius="8"
                HasShadow="False">
                <Entry
                    x:Name="CantidadEntry"
                    BackgroundColor="Transparent"
                    Keyboard="Numeric"
                    Placeholder="Cantidad"
                    PlaceholderColor="#7d7c7c"
                    TextColor="#cccccc" />
            </Frame>

            <!--  Botón Agregar  -->
            <Button
                Padding="15"
                BackgroundColor="#4CAF50"
                Clicked="OnAgregarVentaClicked"
                CornerRadius="8"
                FontSize="16"
                Text="Agregar"
                TextColor="White" />

            <!--  Botón Cancelar  -->
            <Button
                Padding="15"
                BackgroundColor="#FF5722"
                Clicked="Cancelar"
                CornerRadius="8"
                FontSize="16"
                Text="Cancelar"
                TextColor="White" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>