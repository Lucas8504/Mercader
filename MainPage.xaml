<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mercader.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Mercader"
    BackgroundColor="#1e1e1e">

    <ScrollView>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Botones de Acción  -->
            <!--  Botones principales  -->
            <StackLayout
                Grid.Row="0"
                HorizontalOptions="Center"
                Orientation="Horizontal"
                Spacing="15">
                <Button
                    BackgroundColor="#1f6bc2"
                    Clicked="InAgregarEncargo"
                    CornerRadius="10"
                    Text="➕ Encargo"
                    TextColor="White" />
                <Button
                    BackgroundColor="#2e9449"
                    Clicked="InAgregarVenta"
                    CornerRadius="10"
                    Text="💰 Venta"
                    TextColor="White" />
                <Button
                    BackgroundColor="#6e0a24"
                    Clicked="InAgregarGasto"
                    CornerRadius="10"
                    Text="💸 Gasto"
                    TextColor="White" />
            </StackLayout>
            <!--  Resumen financiero  -->
            <Frame
                Grid.Row="1"
                Padding="15"
                BackgroundColor="#2a2a2a"
                CornerRadius="10">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label
                        x:Name="EncargosLabel"
                        Grid.Column="0"
                        FontSize="14"
                        HorizontalOptions="Center"
                        Text="{Binding Source={x:Reference EncargosLabel}, Path=Text}"
                        TextColor="#1f6bc2" />
                    <Label
                        x:Name="VentasLabel"
                        Grid.Column="1"
                        FontSize="14"
                        HorizontalOptions="Center"
                        Text="{Binding Source={x:Reference VentasLabel}, Path=Text}"
                        TextColor="#2e9449" />
                    <Label
                        x:Name="GastosLabel"
                        Grid.Column="2"
                        FontSize="14"
                        HorizontalOptions="Center"
                        Text="{Binding Source={x:Reference GastosLabel}, Path=Text}"
                        TextColor="#6e0a24" />
                    <Label
                        x:Name="GananciasLabel"
                        Grid.Column="3"
                        FontAttributes="Bold"
                        FontSize="14"
                        HorizontalOptions="Center"
                        Text="{Binding Source={x:Reference GananciasLabel}, Path=Text}"
                        TextColor="#1f6bc2" />
                </Grid>
            </Frame>



            <!--  Botón de Exportar a Excel  -->
            <Button
                Grid.Row="5"
                Grid.ColumnSpan="3"
                Margin="20"
                Padding="12"
                BackgroundColor="#6490E9"
                Clicked="OnExportarAExcelClicked"
                CornerRadius="8"
                FontSize="16"
                Text="Exportar a Excel"
                TextColor="#1e1e1e" />
        </Grid>

    </ScrollView>
</ContentPage>