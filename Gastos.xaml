<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mercader.Gastos"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Mercader"
    Title="Gastos"
    BackgroundColor="#1e1e1e">
    <!--  Fondo oscuro para combinar con MainPage  -->

    

    <ScrollView>
        <VerticalStackLayout
            Margin="10"
            Padding="10"
            Spacing="15">



            <!--  Encabezados de las columnas  -->
            <Frame
                Padding="10"
                BackgroundColor="#2a2a2a"
                CornerRadius="8"
                HasShadow="False">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label
                        Grid.Column="0"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        Text="Descripción"
                        TextColor="#cccccc" />
                    <Label
                        Grid.Column="1"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        Text="Monto"
                        TextColor="#cccccc" />
                    <Label
                        Grid.Column="2"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        Text="Cantidad"
                        TextColor="#cccccc" />
                    <Label
                        Grid.Column="3"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        Text="Fecha"
                        TextColor="#cccccc" />
                </Grid>
            </Frame>

            <!--  Lista de gastos  -->
            <CollectionView
                x:Name="GastosCollectionView"
                SelectionChanged="OnSelectionChanged"
                SelectionMode="Single">
                <!--  Espacio entre filas  -->

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem
                                        BackgroundColor="#4CAF50"
                                        Invoked="OnEditSwipeItemInvoked"
                                        Text="Editar" />
                                    <SwipeItem
                                        BackgroundColor="#FF5722"
                                        Invoked="OnDeleteSwipeItemInvoked"
                                        Text="Eliminar" />
                                </SwipeItems>
                            </SwipeView.RightItems>

                            <!--  Fila de la lista  -->
                            <Frame
                                Padding="10"
                                BackgroundColor="{Binding ., Converter={StaticResource AlternatingRowColorConverter}}"
                                CornerRadius="8"
                                HasShadow="False">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Label
                                        Grid.Column="0"
                                        HorizontalTextAlignment="Center"
                                        Text="{Binding Descripcion}"
                                        TextColor="#cccccc" />
                                    <Label
                                        Grid.Column="1"
                                        HorizontalTextAlignment="Center"
                                        Text="{Binding Monto}"
                                        TextColor="#cccccc" />
                                    <Label
                                        Grid.Column="2"
                                        HorizontalTextAlignment="Center"
                                        Text="{Binding Cantidad}"
                                        TextColor="#cccccc" />
                                    <Label
                                        Grid.Column="3"
                                        HorizontalTextAlignment="Center"
                                        Text="{Binding Fecha, StringFormat='{0:dd/MM/yy}'}"
                                        TextColor="#cccccc" />
                                </Grid>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>