<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mercader.DetalleGasto"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Detalle del Gasto"
    BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource PageBackgroundDark}}">

    <ScrollView>
        <VerticalStackLayout
            Margin="20"
            Padding="20"
            Spacing="20">

            <!-- Card con los detalles -->
            <Frame
                Padding="20"
                BackgroundColor="{AppThemeBinding Light={StaticResource CardAltBackground}, Dark={StaticResource CardAltBackgroundDark}}"
                CornerRadius="12"
                HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Descripción -->
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            Text="Descripción:"
                            TextColor="#4CAF50" />
                        <Label
                            x:Name="LabelDescripcion"
                            FontSize="18"
                            Text="{Binding Descripcion}"
                            TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                    </VerticalStackLayout>

                    <!-- Monto por unidad y Cantidad en una fila -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Monto por unidad -->
                        <VerticalStackLayout Grid.Column="0" Spacing="5">
                            <Label
                                FontAttributes="Bold"
                                FontSize="16"
                                Text="Monto por Unidad:"
                                TextColor="#4CAF50" />
                            <Label
                                x:Name="LabelMonto"
                                FontSize="18"
                                FontAttributes="Bold"
                                Text="{Binding Monto, StringFormat='${0:F2}'}"
                                TextColor="#FFC107" />
                        </VerticalStackLayout>

                        <!-- Cantidad -->
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label
                                FontAttributes="Bold"
                                FontSize="16"
                                Text="Cantidad:"
                                TextColor="#4CAF50" />
                            <Label
                                x:Name="LabelCantidad"
                                FontSize="18"
                                FontAttributes="Bold"
                                Text="{Binding Cantidad}"
                                TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Total -->
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            Text="Total:"
                            TextColor="#4CAF50" />
                        <Label
                            x:Name="LabelTotal"
                            FontSize="20"
                            FontAttributes="Bold"
                            TextColor="#FFC107" />
                    </VerticalStackLayout>

                    <!-- Fecha del gasto -->
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            Text="Fecha del Gasto:"
                            TextColor="#4CAF50" />
                        <Label
                            x:Name="LabelFecha"
                            FontSize="16"
                            Text="{Binding Fecha, StringFormat='{0:dd/MM/yyyy}'}"
                            TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>

            <!-- Botones de acción -->
            <VerticalStackLayout Spacing="15">

                <!-- Botón Editar -->
                <Button
                    x:Name="BtnEditar"
                    BackgroundColor="#2196F3"
                    Clicked="OnEditarClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="✏️ Editar Gasto"
                    TextColor="White" />

                <!-- Botón Eliminar -->
                <Button
                    x:Name="BtnEliminar"
                    BackgroundColor="#FF5722"
                    Clicked="OnEliminarClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="🗑️ Eliminar Gasto"
                    TextColor="White" />

                <!-- Botón Volver -->
                <Button
                    x:Name="BtnVolver"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray200}}"
                    Clicked="OnVolverClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="⬅️ Volver"
                    TextColor="White" />

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>