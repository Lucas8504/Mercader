<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mercader.DetalleVenta"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:domain="clr-namespace:Mercader.Domain.Entities;assembly=Mercader.Core"
    Title="Detalle de la Venta"
    BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource PageBackgroundDark}}">

    <ScrollView>
        <VerticalStackLayout
            Margin="20"
            Padding="20"
            Spacing="20">

            <!-- Card con los detalles -->
            <Frame
                Padding="20"
                BackgroundColor="{AppThemeBinding Light={StaticResource CardAltBackground}, Dark={StaticResource CardAltBackgroundDark}}"
                CornerRadius="12"
                HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Descripción -->
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            Text="Descripción:"
                            TextColor="#4CAF50" />
                        <Label
                            x:Name="LabelDescripcion"
                            FontSize="18"
                            Text="{Binding Descripcion}"
                            TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                    </VerticalStackLayout>

                    <!-- Fecha de la venta -->
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            Text="Fecha de la Venta:"
                            TextColor="#4CAF50" />
                        <Label
                            x:Name="LabelFecha"
                            FontSize="16"
                            Text="{Binding Fecha, StringFormat='{0:dd/MM/yyyy}'}"
                            TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>

            <!-- ===== ARTÍCULOS ===== -->
            <Label
                FontAttributes="Bold"
                FontSize="16"
                Text="Artículos:"
                TextColor="#4CAF50" />

            <VerticalStackLayout
                x:Name="ArticulosStack"
                Spacing="6">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="domain:ArticuloVenta">
                        <Frame
                            Padding="10"
                            BackgroundColor="{AppThemeBinding Light={StaticResource CardAltBackground}, Dark={StaticResource CardAltBackgroundDark}}"
                            CornerRadius="8"
                            HasShadow="False">
                            <Grid
                                ColumnDefinitions="*,Auto,Auto"
                                RowDefinitions="Auto,Auto"
                                ColumnSpacing="8"
                                RowSpacing="4">
                                <!-- Descripción -->
                                <Label
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="3"
                                    FontSize="15"
                                    FontAttributes="Bold"
                                    Text="{Binding Descripcion}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                                <!-- Precio Unitario -->
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    FontSize="13"
                                    Text="{Binding PrecioUnitario, StringFormat='P/U: ${0:F2}'}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"
                                    VerticalOptions="Center" />
                                <!-- Cantidad -->
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    FontSize="13"
                                    Text="{Binding Cantidad, StringFormat='{0} uds.'}"
                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"
                                    VerticalOptions="Center" />
                                <!-- Total del artículo -->
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="2"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    Text="{Binding Total, StringFormat='${0:F2}'}"
                                    TextColor="#FFC107"
                                    VerticalOptions="Center" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>

            <!-- Total calculado -->
            <Frame
                Padding="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource CardAltBackground}, Dark={StaticResource CardAltBackgroundDark}}"
                CornerRadius="8"
                HasShadow="False">
                <Grid ColumnDefinitions="Auto,*,Auto">
                    <Label
                        Grid.Column="0"
                        FontAttributes="Bold"
                        FontSize="16"
                        Text="Total:"
                        TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}"
                        VerticalOptions="Center" />
                    <Label
                        x:Name="LabelTotal"
                        Grid.Column="2"
                        FontAttributes="Bold"
                        FontSize="18"
                        Text="$0"
                        TextColor="#FFC107"
                        VerticalOptions="Center" />
                </Grid>
            </Frame>

            <!-- Botones de acción -->
            <VerticalStackLayout Spacing="15">

                <!-- Botón Editar -->
                <Button
                    x:Name="BtnEditar"
                    BackgroundColor="#2196F3"
                    Clicked="OnEditarClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="✏️ Editar Venta"
                    TextColor="White" />

                <!-- Botón Eliminar -->
                <Button
                    x:Name="BtnEliminar"
                    BackgroundColor="#FF5722"
                    Clicked="OnEliminarClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="🗑️ Eliminar Venta"
                    TextColor="White" />

                <!-- Botón Volver -->
                <Button
                    x:Name="BtnVolver"
                    BackgroundColor="{AppThemeBinding Light={StaticResource ButtonSecondaryLight}, Dark={StaticResource ButtonSecondaryDark}}"
                    Clicked="OnVolverClicked"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    Text="⬅️ Volver"
                    TextColor="White" />

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>